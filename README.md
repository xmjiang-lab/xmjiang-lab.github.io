# Jiang Lab @ SISU — Lab website

Website for the **Jiang Lab** at the **Institute of Language Sciences (ILAS)**,
Shanghai International Studies University (SISU).

**PI:** Prof. Xiaoming Jiang (蒋晓鸣)
**Affiliation:** Research Center of Psycholinguistics & Neurolinguistics, ILAS, SISU

This site replaces / complements the existing
[xiaomingjiang.wordpress.com](https://xiaomingjiang.wordpress.com/) with:

- A proper SISU-branded landing page (in line with the SISU Visual Identity)
- Bilingual content (中文 / English), zero-reload language switch
- Auto-updating publications list (synced from ORCID)
- A dedicated **Facilities** page that surfaces the lab's hardware — the
  single biggest selling point for international collaborators
- A **News & Recruitment** page (CSP fellowship, Shanghai Sci-tech
  Co-research Program, etc.)

---

## Repository structure

```
.
├── index.html            Home (hero + intro + sidebar)
├── people.html
├── projects.html
├── publications.html
├── facilities.html
├── news.html
├── contact.html
├── assets/
│   ├── css/style.css     SISU Blue + yellow accent
│   ├── js/main.js        i18n + data-driven renderers
│   ├── data/
│   │   ├── i18n-zh.json
│   │   ├── i18n-en.json
│   │   ├── people.json         current + alumni + author_patterns
│   │   ├── projects-data.json
│   │   ├── publications.json   ← regenerated by fetch_orcid.py
│   │   ├── facilities.json
│   │   └── news.json
│   └── images/
│       ├── logos/        SISU + ILAS + STCSM (placeholder SVGs — replace with real ones)
│       └── people/       member photos
├── scripts/
│   └── fetch_orcid.py    ORCID sync → site_data.xlsx → publications.json
├── site_data.xlsx        (auto-created on first sync — manual fields layer)
└── README.md
```

## Updating content

### People / projects / facilities / news
Edit the matching JSON file in `assets/data/`. No build step. Reload the page.

### Publications

```bash
pip install requests openpyxl
python scripts/fetch_orcid.py
```

This:
1. fetches works from every ORCID listed in `LAB_ORCIDS` at the top of the script,
2. merges and de-duplicates by DOI (falls back to title prefix),
3. **preserves manual fields** (`pdf_url`, `code_url`, `video_url`, `category`,
   `show`, `group_id`, `notes`) that you've added in `site_data.xlsx`,
4. regenerates `assets/data/publications.json`.

**Before first run:** update `LAB_ORCIDS` in `scripts/fetch_orcid.py` with
Prof. Jiang's actual ORCID iD.

Add new lab members to the ORCID sync by appending to `LAB_ORCIDS`. To bold
their name on the site, also add a regex source string to `author_patterns`
in `assets/data/people.json` — no JavaScript change needed.

### Highlighting a paper as "Chinese journal"
ORCID doesn't tag this. Open `site_data.xlsx`, set the `category` cell to
`chinese` for the relevant row, save, and re-run the sync (or just edit
`publications.json` directly).

### Hiding a paper without deleting it
Set `show = no` in `site_data.xlsx`.

## Local preview

```bash
python -m http.server 4000
# then open http://localhost:4000
```

## Visual identity

The stylesheet uses the **SISU Visual Identity System**:
- Primary: **SISU Blue** `#003f88` (sea & sky — the official school standard)
- Accent: **bright yellow** `#fcb813` (one of SISU's auxiliary colors)
- Type: serif (Source Han Serif SC / Songti SC / Georgia) for titles,
  sans-serif (PingFang SC / Microsoft YaHei / Helvetica) for body.

Placeholder SVG logos are included in `assets/images/logos/`. Replace
`sisu.svg` and `sisu-white.svg` with the official SISU mark before going
public (and confirm SISU branding permission for an external lab site).

## License

© 2026 Jiang Lab, Institute of Language Sciences, SISU. All rights reserved.
